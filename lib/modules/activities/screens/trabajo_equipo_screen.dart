import 'package:flutter/material.dart';

class TrabajoEnEquipoScreen extends StatelessWidget {
  const TrabajoEnEquipoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Trabajo en equipo')),
      body: const Center(child: Text('Contenido de trabajo en equipo')),
    );
  }
}
